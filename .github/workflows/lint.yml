name: Lint

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: 'Install dependencies'
        run: npm i
      - name: 'Run lint'
        run: npm run lint